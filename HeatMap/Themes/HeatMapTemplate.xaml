<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:heatMap="clr-namespace:HeatMap">
    <Style TargetType="{x:Type heatMap:HeatMapControl}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type heatMap:HeatMapControl}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="10*" />
                            <RowDefinition Height="1*" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="10*" />
                            <ColumnDefinition Width="5*" />
                            <ColumnDefinition Width="30*" />
                        </Grid.ColumnDefinitions>
                        <Canvas x:Name="PART_CoordinateSystemCanvas" Grid.Row="0" Grid.RowSpan="2" Grid.Column="0"
                                Background="Beige" />
                        <Border Grid.Row="0" Grid.Column="2" Padding="5">
                            <heatMap:HeatMapVisualHost x:Name="PART_HeatMapVisualHost" />
                        </Border>
                        <Border Grid.Row="0" Grid.Column="1" Background="Transparent" Padding="0,5">
                            <Canvas x:Name="PART_CoordinateYCanvas" />
                        </Border>
                        <Border Grid.Row="1" Grid.Column="2" Background="Transparent" Padding="5,0">
                            <Canvas x:Name="PART_CoordinateXCanvas" />
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>